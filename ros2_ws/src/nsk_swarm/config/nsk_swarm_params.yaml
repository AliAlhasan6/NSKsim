/**:
  ros__parameters:
    num_robots:              8
    comm_range:              3.0       # metres, 2D Euclidean
    share_interval:          8.0       # seconds between broadcasts
    zmq_endpoint:            "ipc:///tmp/nsk_engine_0"
    nsk_base_path:           "/home/lawlite/Desktop/NSK"
    checkpoint_path:         "experiments/checkpoints/joint_best.pt"
    config_path:             "configs/base.yaml"
    dataset_indices:         [0, 570, 1140, 1710, 2280, 2850, 3420, 3990]
    world_size:              20.0      # square world side length in metres
    walk_speed:              0.15      # m/s linear velocity
    walk_turn_max:           0.5       # rad/s max angular velocity
    monitor_interval:        10.0      # seconds between convergence reports
    convergence_threshold:   0.25      # target mean pairwise similarity
    zmq_timeout_ms:          2000      # ZMQ request timeout in milliseconds
